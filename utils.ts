import { Habit } from './types';

// Utility kept for potential future expansion, currently simple
export const getAllHabitStats = (habits: Habit[], todayStr: string) => {
  // Placeholder if needed for future analytics
  return [];
};